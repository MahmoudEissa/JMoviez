# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'JMoveiz' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for JMoveiz
    pod 'ESNetworkManager'
    pod 'RealmSwift'
    pod 'Kingfisher', '~> 7.0'
    target 'JMoveizTests' do
           inherit! :search_paths
    end
end
